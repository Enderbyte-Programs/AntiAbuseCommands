give:
  enabled: true
  worlds:
    - old_server
    - old_server_nether
    - old_server_the_end
    - parkour
  same-world-only: true
  to-self-only: false
  forbidden:
    - command_block
    - repeating_command_block
    - chain_command_block

teleport:
  enabled: true
  worlds:
    - old_server
    - old_server_nether
    - old_server_the_end
    - parkour
  same-world-only: false
  involve-self-only: true

gamemode:
  enabled: true
  worlds:
    - old_server
    - old_server_nether
    - old_server_the_end
    - parkour
  same-world-only: false
  involve-self-only: false

multiverse:
  enabled: true
  block-worlds:
    - examplenether
  block-player-tp: true

transfer:
  enabled: true
  block-other-players: true

summon:
  enabled: true
  forbidden:
    - command_block_minecart
    - falling_block
  strict-find: true

settitle:
  enabled: true
  allowed:
    - written_book
    - writable_book

setlore:
  enabled: true
  allowed:
    - written_book
    - writable_book

whitelist-commands:
  #These commands will be ignored and not processed
  - tpa
  - tpahere
  - tpcancel
  - tpaccept
  - tpdeny